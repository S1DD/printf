#include "main.h"

/**
 * handle_percent - Handles the '%' format specifier.
 * Return: Always returns 1 as '%' is a single character.
 */
int handle_percent(void)
{
    put_char('%');
    return (1);
}
